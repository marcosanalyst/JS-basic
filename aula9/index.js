// ECMAScript

var nomeTeste = 'Luiz';
// redeclarando a variável, não criar variável var!!
var nomeTeste = 'Otávio';
console.log(nomeTeste);

// TIPOS PRIMITIVOS: String, number, undefined, null e boolean

// String
const nome = 'Luiz';
const nome2 = "Marcos";
const nome3 = `Jorte`;

// Number
const num1 = 10;
const num2 = 10.52;

// Undefined
let nomeAluno; // undefined = não aponta para nenhum local na memória

// Null
let sobreAluno = null; // Nulo -> não aponta para nenhum local na memória

// Boolean
const aprovado = true;
const brasileiro = false;

console.log(typeof nome); // tipo da variável e valor da variável

